# IGV Hope — igvhope.com

Static site (self-contained HTML), migrated off Webflow.

This is a raw import of the pre-Git working folder, kept as-authored for
now. It has **not yet** been restructured into the `public/` + `workers/`
layout used by the other IGV property repos ([IGVcapital-website](https://github.com/IGVhousing/IGVcapital-website),
[IGVhousing-website](https://github.com/IGVhousing/IGVhousing-website)) —
that pass (root-absolute asset paths, deduped images, page slugs matching
nav links, Cloudflare Pages/Worker config) is a follow-up.
